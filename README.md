# AndroidLifecycle

### This project consist of small portion of android lifecycle which can be shown during running state in logcat.
The layout looks as follows:


![](https://github.com/AnilThapa5/AndroidLifecycle/blob/master/images/lifecycle.PNG)


But the layout cannot represent the lifecycle but when different steps are conducts it caputure in debug section.
